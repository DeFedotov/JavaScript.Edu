<!DOCTYPE HTML>
<html>
<body>

  <script>
    'use strict'

    //let year = prompt("What year did ECMAScript-2015 appear?", "");
    //if (year == 2015) {
    //  alert("You are right!");
    //  alert("You are so smart!");
    //} else {
    //  alert("That's wrong!");
    //}

/*
    let year = prompt("What year did ECMAScript-2015 appear?", "");
    if (year < 2015) {
      alert("Too early...");
    } else if (year > 2015) {
      alert("Too late.")
    } else {
      alert("That's right!");
    }
*/

/*
    let accessAllowed;
    let age = prompt('How old are you?','');

    if (age > 18) {
      accessAllowed = true;
    } else {
      accessAllowed = false;
    }

    alert(accessAllowed);
*/

/*
    let age = prompt('Age?', 18);

    let message = (age < 3) ? 'Hello, baby!' :
        (age < 18) ? 'Hello!' :
        (age < 100) ? 'Good afternoon!' :
        'What an unusual age!';

    alert(message);
*/

//  tasks

/*
      let question = prompt("What is an official name of JavaScript?", "");

      if (question == "ECMAScript") {
        alert("Right!");
      } else {
        alert("Don't you know? ECMAScript!");
      }
*/

/*
    let number = prompt("Enter your number", 0);

    if (number > 0) {
      alert(1);
    } else if (number < 0) {
      alert(-1);
    } else {
      alert(0);
    }
*/

    
  </script>

</body>
</html>
